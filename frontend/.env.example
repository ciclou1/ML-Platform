# 前端环境变量示例。复制为 .env.development（本地）或 .env.production（构建）后按需修改。
#
# 开发环境：默认不设置 VITE_API_BASE_URL，请求走 Vite 的 /api 代理到后端。
# 生产环境：若前端与后端不在同源，请设置为后端根地址（不含 /api/v1）。
# VITE_API_BASE_URL=http://localhost:8000
